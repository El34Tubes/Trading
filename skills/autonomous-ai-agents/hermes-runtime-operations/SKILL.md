---
name: hermes-runtime-operations
description: "Operate and validate a live Hermes Agent install: doctor/status checks, optional dependency installation, runtime venv package fixes, and smoke-test verification."
version: 1.0.0
author: Hermes Agent
license: MIT
metadata:
  hermes:
    tags: [hermes, operations, validation, dependencies, troubleshooting]
    related_skills: [hermes-agent, scheduled-market-research-reports]
    created_by: agent
---

# Hermes Runtime Operations

Use this skill when the user asks to validate what is working/not working in a live Hermes Agent install, install optional/system dependencies, or verify that a Hermes runtime feature works after setup changes.

This is an operations/checklist skill, not a replacement for the bundled `hermes-agent` reference skill. Load `hermes-agent` first for authoritative CLI/config commands, then use this skill for the practical validation workflow.

## Runtime Discovery Notes

- For user questions about "crontab" on a Hermes host, check both the literal Linux crontab and Hermes's own scheduler; see `references/hermes-scheduler-vs-system-crontab-and-ttyd-browser.md`.
- For browser-visible hosted Hermes setups, look for a Traefik-routed `ttyd` container endpoint before assuming there is no browser UI; see `references/hermes-scheduler-vs-system-crontab-and-ttyd-browser.md`.

## Workflow

1. Establish baseline health.
   - Run `hermes status --all` to see provider, gateway, platform, and high-level environment status.
   - Run `hermes doctor` for dependency/config/tool availability diagnostics.
   - Run quick direct probes for the items doctor complains about, e.g. `node --version`, `npm --version`, `rg --version`, or a Python import in the Hermes venv.

2. Separate installable dependencies from credential/config gaps.
   - Installable examples: `nodejs`, `npm`, `ripgrep`, optional Python packages such as `python-telegram-bot`.
   - Credential/config examples: API keys, OAuth login, provider-specific tokens, Home Assistant/Spotify/Feishu setup, CDP endpoint configuration.
   - Do not describe unconfigured credentials as broken tools; report them as needing setup.

3. Install OS packages with the system package manager.
   - On Ubuntu/Debian:
     ```bash
     apt-get update
     DEBIAN_FRONTEND=noninteractive apt-get install -y nodejs npm ripgrep
     ```
   - Verify immediately:
     ```bash
     node --version
     npm --version
     rg --version | head -1
     ```

4. Install optional Python packages into the Hermes runtime venv, not an unrelated system Python.
   - Resolve the Hermes venv path from the install; common path:
     `/usr/local/lib/hermes-agent/venv/bin/python`
   - Prefer `uv pip install --python /path/to/venv/bin/python <package>`.
   - If `apt-get install python3-<pkg>` makes `/usr/bin/python3` work but Hermes cron/tools still fail, the missing package is in the wrong Python; install into the Hermes venv and verify with that exact interpreter.
   - Example:
     ```bash
     uv pip install --python /usr/local/lib/hermes-agent/venv/bin/python python-telegram-bot
     /usr/local/lib/hermes-agent/venv/bin/python -c 'import telegram; print(telegram.__version__)'
     ```
   - For HTML parsing dependencies such as `bs4`, see `references/hermes-runtime-optional-python-deps.md`.

5. Let Hermes auto-fix its own command/path issues.
   - Run `hermes doctor --fix` after installing dependencies.
   - This can repair items like a missing `~/.local/bin/hermes` symlink.
   - If `hermes doctor` reports `Skills Hub directory not initialized`, run `hermes skills list` once and then re-run `hermes doctor`; this initializes the hub/lock state without changing installed skills.

6. Verify with both diagnostics and live smoke tests.
   - Re-run `hermes doctor`.
   - For profile-scoped cron operations, check the profile that owns the jobs explicitly (`hermes --profile <name> cron list --all`). Do not rely only on the active profile: scheduled ops jobs may run from a Mike/admin profile while production jobs live under `default`, `clerky`, or another worker profile. If an ops pre-run/triage script only prints the active profile's empty cron list, fix the script to include the production profile cron listing as deterministic context so future runs do not rediscover or misreport the same issue.
   - For script-only/no-agent watchdogs, empty stdout with exit code 0 is normally a healthy silent run. Report only nonzero exits or actionable output. If an aggregate usage snapshot emits a threshold line but the dedicated usage-limit watchdog runs silently twice and scoped auth is healthy, treat it as a usage-volume warning, not an active quota/rate-limit incident; do not pause jobs unless the watchdog/logs show a real limit event.
   - When checking cron scheduler health around a just-due run, do not rely on `hermes cron status` alone if it still shows a stale `Next run` timestamp. Cross-check `hermes --profile <name> cron list --all`, cron output/session creation for the specific job ID, and gateway/agent log lines before reporting a stuck scheduler; if a due job is only a minute or two stale, wait/poll once rather than declaring failure. If an LLM-driven cron job is currently running on the cron ticker, just-due no-agent/script jobs may remain listed at their old `Next run` until that active run exits; inspect `agent.log` for the active `cron_<job_id>...` session and only classify the scheduler as stuck if the due jobs remain stale after the active run finishes. If `cron status` remains stale but gateway logs show only heartbeat/memory-monitor lines, also inspect `/root/.hermes/cron/.tick.lock` age and the gateway process/threads before taking action; a zero-byte tick lock with no matching cron session can be a lead, but treat it as unresolved scheduler triage unless it persists past the next poll or blocks jobs. For default-profile Wolfy jobs, check both `/root/.hermes/cron/.tick.lock` and `/root/.hermes/profiles/default/cron/.tick.lock`; the active lock may still live in the global cron directory even when commands are run with `--profile default`. See `references/wolfy-embedding-and-ad-hoc-query-triage-2026-06-19.md`, `references/wolfy-cron-stale-tick-lock-triage-2026-06-20.md`, and `references/wolfy-cron-tick-lock-and-visible-ledger-smoke-2026-06-20.md`.
   - For read-only Wolfy visible-progress/status helpers used by several cron/planner probes, prefer tolerant CLI compatibility over brittle one-off invocations: preserve legacy flags, add natural aliases such as `--format markdown|json` and compact-output options such as `--limit N`, keep the helper strictly read-only, restore executable mode when direct invocation is expected, and verify compile + Markdown + JSON + two silent autorepair runs. See `references/wolfy-visible-progress-ledger-cli-compat-2026-06-20.md`.
   - Avoid piping live Hermes/cron/log output directly into an interpreter for ad-hoc filtering (for example `hermes ... | python3 - <<'PY'`): Tirith may block it as pipe-to-interpreter. Use plain CLI output, write to a temp file and inspect it, or use file/search tools for filtering.
   - For Postgres no-agent helpers that update rows via `psycopg`, explicitly `conn.commit()` before exit and verify persistence with a follow-up `psql` query plus a second silent run. Do not trust a `RETURNING` printout alone; uncommitted subprocess updates can look successful while leaving rows unchanged.
   - For Postgres-backed coordination ledgers, watch for context generators that repeatedly re-select stale local `in_progress` work and create duplicate `blocked`/`duplicate-or-already-claimed` `agent_runs`. The safe fix is usually to have context generators claim only fresh queued work, leave stale `in_progress` cleanup to the watchdog, and verify with a post-fix query such as `count(*) where error_message='duplicate-or-already-claimed' and started_at > <fix time>`.
   - If an LLM-driven cron job times out before it can finish its Postgres `agent_runs` row, close only the stale run row as `blocked` with `records_created=0` and a specific timeout/config summary; do not fabricate the missed report or mark it completed. Before changing models, inspect the current cron job config because the historical `last_error` may mention startup/default model text that differs from the saved per-job fallback. If the job already has a per-job fallback model (for example `model: gpt-5.4`) but the saved `last_error` still names the default model (`gpt-5.5`), treat the error text as stale/contradictory until verified: inspect `cron/jobs.json` or `hermes --profile <name> cron list --all`, then run a direct one-shot smoke with that exact profile/model (for example `hermes --profile default chat -Q -m gpt-5.4 -q 'Runtime smoke: reply with exactly OK.'`). Do not change cron models or alert the user if the context smoke, model smoke, watchdogs, and coordination ledger are healthy. Verify `stale_started_runs=0`, rerun script-only smokes/autorepair, and report the next scheduled retry separately from the closed DB run. See `references/wolfy-alpha-search-timeout-stale-run-cleanup-2026-06-25.md` and `references/wolfy-alpha-search-stale-model-error-smoke-2026-06-25.md`.
   - For Postgres `knowledge_chunks` ad-hoc diagnostic drift, if probes expect a top-level `title` column, add a nullable compatibility alias populated from `metadata->>'title'`, `metadata->>'source_title'`, linked `agent_artifacts.title`, or `source_table || ':' || source_id`. Preserve it in both `postgres_init.sql` and `mike_safe_autorepair.py`, add a `BEFORE INSERT OR UPDATE` trigger, and verify `count(title)=count(*)` plus the exact failing query. See `references/wolfy-knowledge-chunks-title-alias-2026-06-15.md`.
   - For context-script smoke tests that intentionally call helpers which create `agent_runs.status='started'` rows or claim temporary tasks, finish the temporary smoke rows explicitly with `records_created=0` and a smoke-test summary so the operations pass does not introduce stale-run noise; verify `agent_runs`/`agent_tasks` status counts afterward. See `references/wolfy-context-smoke-cleanup-2026-06-02.md`.
   - For CLI smoke runners that are advertised as safe on a fresh `/tmp` fixture DB, verify the exact CLI invocation on a brand-new path, not only the pytest fixture path. If pytest passes but the CLI fails with missing fixture tables, add deterministic fixture seeding inside the runner for the supplied temp DB only, keep Postgres writes stubbed/captured, and assert the expected row deltas from the emitted JSON. See `references/wolfy-intraday-scanner-wrapper-and-accountability-smoke-2026-06-04.md`.
   - When a context helper is routinely probed by operations but normally opens an `agent_runs.status='started'` row for the downstream LLM to close, add an explicit smoke-only mode such as `WOLFY_CONTEXT_SMOKE=1` that prints the same deterministic context without opening a run row. The smoke guard must run before `claim_next_task()`, `start_agent_run()`, task status updates, or any claim-token creation; smoke tests should not claim queued work at all. Verify with before/after counts for both started runs and `agent_tasks.status='in_progress'`, and close/requeue any accidental smoke-created run/task rows with `records_created=0`. See `references/wolfy-context-smoke-mode-no-started-runs-2026-06-04.md` and `references/wolfy-jonah-context-smoke-no-claim-2026-06-19.md`.
   - For Wolfy context helpers that dedupe by `agent_tasks.source_fingerprint`, do not record `blocked/duplicate-or-already-claimed` rows when the deduped task is already `completed`; treat that as a completed no-op with `records_created=0`. Only leave `blocked/duplicate-or-already-claimed` for genuinely active claims or blocked tasks. Ops smoke probes should use `WOLFY_CONTEXT_SMOKE=1` so they do not claim Sentinel/Yang work; if a manual smoke accidentally claims a task, immediately return it to `queued` and mark the smoke run completed. See `references/wolfy-context-dedupe-completed-noop-2026-06-14.md`.
   - For durable Wolfy SQLite schema drift, prefer non-destructive compatibility aliases over destructive rewrites. If newer diagnostics query aliases such as `strategy_rules.name/status/asset_class`, add nullable/default alias columns, backfill from canonical fields, add idempotent init-script guards/triggers, and verify the exact query plus the affected cron context. See `references/wolfy-runtime-compatibility-aliases.md`.
   - For Wolfy Postgres ad-hoc query drift, use the same non-destructive alias pattern rather than changing canonical write paths. Common probe aliases now include `agent_runs.completed_at` mirrored from `ended_at`, `agent_runs.result_summary` mirrored from `summary`, `agent_tasks.summary` mirrored from `description`, `agent_tasks.error_message` populated from blocked-task summaries/descriptions and by `block_task()`, `agent_tasks.payload` as a JSONB convenience mirror of the task row, `agent_tasks.source_table/source_id` backfilled from `payload`, `source_fingerprint`, or `id`, `agent_tasks.type` mirrored from `task_type`, `agent_artifacts.source_final_url` mirrored from canonical `source_url` for probes that expect resolved/final URL fields, `strategy_rules.scope` mirrored from `status` in the read-only compatibility view, `alpha_leads.company_name/scanner_type/score` mirrored from `raw_payload`/`lead_type`/`evidence_quality_score`, `alpha_leads.scanner_run_id` backfilled from numeric `raw_payload->>'scanner_run_id'` / `scanner_run` / `run_id`, `alpha_leads.market_context` mirrored from `raw_payload->'market_context'`, `alpha_leads.rationale/summary` mirrored from thesis/raw payload/title, `scanner_results.company_name/scanner_type` mirrored from `notes`, and `scanner_results.signal` mirrored from `notes->>'signal'`/scanner type. If probes query retired names, prefer compatibility views (for example `alpha_search_leads AS SELECT ... FROM alpha_leads`) over duplicate mutable tables; include newly needed aliases such as `scanner_run_id` in the view. Scanner factor aliases may also be needed when probes expect top-level `scanner_results.rs_spy_20/rs_qqq_20/breakout_20d_pct/volume_surge_* /atr_pct/squeeze_* /liquidity_spread_proxy/trend_regime/rank_reasons/gap_reversal_flag`; backfill those from `scanner_results.notes` JSONB, preserve them in `mike_safe_autorepair.py`, `postgres_init.sql`, and `wolfy_postgres_pipeline.py`, then verify the exact failing query. If `recommendation_reviews.recommendation_id` is text and joins to `recommendations.id` fail with `operator does not exist: text = bigint`, first confirm values are numeric, then use `ALTER TABLE ... ALTER COLUMN recommendation_id TYPE BIGINT USING recommendation_id::bigint` and preserve that type in init/autorepair. Preserve fixes in `mike_safe_autorepair.py`, `postgres_init.sql`, and any pipeline-local `CREATE TABLE IF NOT EXISTS` initializer such as `alpha_search_pipeline.py`; sync the global/Mike/Clerky autorepair wrappers and verify the second autorepair run is silent plus the exact alias queries work. If the live DB already has the aliases but autorepair/schema files do not, patch the preservation layers anyway so the fix survives future repair/init cycles. For retired SQLite-era `strategy_rules` probes in Postgres-primary operations, prefer a read-only compatibility view over a mutable duplicate table: project canonical `strategies` plus archived `knowledge_chunks(source_table='sqlite.strategy_rules')`, include common probe aliases such as `ticker` when needed, preserve it in `postgres_init.sql` and `mike_safe_autorepair.py`, then verify the exact failing probes. When changing the column list of a Postgres compatibility view, `CREATE OR REPLACE VIEW` may fail with `cannot change name of view column`/`cannot drop columns from view`; for read-only compatibility views use `DROP VIEW IF EXISTS ...; CREATE VIEW ...` instead. When live scanner/report paths are Postgres-only, keep temp SQLite fixture compatibility explicit rather than deleting it: live cron should pass `db_path=None`, while tests that inject a SQLite path/connection may still expect historical `scanner_runs`, `scanner_results.notes_json`, and SQLite freshness semantics. See `references/wolfy-postgres-alias-drift-agent-alpha-scanner-2026-06-12.md`, `references/wolfy-postgres-alias-autorepair-preservation-2026-06-12.md`, `references/wolfy-postgres-strategy-rules-compatibility-view-2026-06-12.md`, `references/wolfy-scanner-results-alias-and-fixture-compat-2026-06-12.md`, `references/wolfy-postgres-sentinel-alpha-search-aliases-2026-06-12.md`, `references/wolfy-postgres-agent-task-and-strategy-rules-aliases-2026-06-14.md`, `references/wolfy-postgres-agent-ledger-aliases-2026-06-18.md`, `references/wolfy-postgres-task-and-alpha-lead-aliases-2026-06-18.md`, and `references/wolfy-postgres-agent-task-type-strategy-scope-aliases-2026-06-19.md`.
   - If Jonah/Wolfy cron/tool output shows SQLite ad-hoc query drift around knowledge tables, add nullable compatibility aliases instead of rewriting canonical schema. For `knowledge_notes`, common aliases include `title/category/note/content/note_type/source_type` mirrored from `topic/tags/summary`; for `knowledge_sources`, `path/url` should mirror `url_or_reference`; for `strategy_rules`, common aliases include `name/status/asset_class/category/source_id/rule_text/is_active` mirrored from `rule_name/implementation_status/rule_type/source_basis/description/enabled`. Backfill rows, preserve the fix in `init_wolfy_db.py` and the canonical global `/root/.hermes/scripts/mike_safe_autorepair.py`, then run that global autorepair wrapper so it syncs `/root/.hermes/wolfy/mike_safe_autorepair.py` plus Mike/Clerky profile copies. Verify a second autorepair run is silent plus the exact previously failing queries work. When updating trigger bodies, drop/recreate compatibility triggers rather than relying on stale `CREATE TRIGGER IF NOT EXISTS` definitions. See `references/wolfy-knowledge-notes-compatibility-aliases-2026-06-04.md`, `references/wolfy-knowledge-alias-drift-note-type-path-2026-06-04.md`, and `references/wolfy-sqlite-compatibility-alias-expansion-2026-06-04.md`.
   - Treat `recent errors` log tails as triage leads, not current truth: historical pytest/tool failures may already be fixed by another worker. Before editing code, rerun the exact relevant tests plus the broader local smoke suite when cheap; if they now pass, do not invent a fix or report stale failures as active blockers. For Jonah/Wolfy scratch `tmp_*.py` warnings, first distinguish ad-hoc research probe mistakes from durable schema drift: rerun the exact script/query, check core Postgres/coordination smokes, and avoid adding compatibility aliases for one-off psycopg placeholder/string-format mistakes. For stale Wolfy wrapper-missing tails, verify the exact live/global/profile wrapper paths, compile them, run `WOLFY_CONTEXT_SMOKE=1` context smokes, and run autorepair twice for silence before reporting; see `references/wolfy-stale-wrapper-error-verification-2026-06-14.md` and `references/wolfy-embedding-and-ad-hoc-query-triage-2026-06-19.md`.
   - For silent scheduled Mike ops passes, validate the minimum live invariants and then suppress delivery if nothing changed: Postgres requirements guard OK, default-profile cron status/list sane, coordination counts show no stale started runs or fresh duplicate-claim noise, autorepair twice is silent, core script-only helpers are silent, context smokes produce output without opening run/task rows, and compile checks pass. Do not turn historical agent-tool warnings (for example failed ad-hoc Jonah scratch probes, patch retries, or skill patch attempts from earlier cron sessions) into a user-facing report when the live smokes are clean; final response should be exactly `[SILENT]` unless a new fix, blocker, or actionable warning remains. If a due no-agent watchdog remains listed as `Next run` while the current LLM ops cron session is still active and `/root/.hermes/cron/.tick.lock` was just touched, treat it as an active-tick artifact: verify direct script smokes and poll after the active run rather than declaring the scheduler stuck. See `references/mike-cron-tick-lock-active-run-and-embedding-closure-2026-06-25.md`.
   - For Wolfy Kanban cards blocked only on `review-required` after a worker handoff, Mike may clear the blocker even if the card is assigned to another profile: rerun the cited non-destructive verification commands, include required guards, comment with real output, and complete the card if everything passes. Prefer read-only smokes (`--json`) over repeat `--store` calls when the handoff already proved storage. Put the review summary in `hermes kanban comment ...`; then run `hermes kanban complete <id>` with no extra free-form summary argument, because extra words can be parsed as IDs/terminal state and produce confusing output even when completion succeeds. After completing any parent blocker, re-list the board and run `hermes kanban dispatch`; verify the expected child moved to `ready`/`running` or report why it did not. For Kanban CLI board selection, put the global board flag before the subcommand (`hermes kanban --board wolfy list --assignee mike`, `hermes kanban --board wolfy dispatch`); `--board` after the subcommand is rejected. Also avoid assuming `list --all` exists — use `--archived`, status/assignee filters, or `stats` for board snapshots. See `references/wolfy-kanban-review-blocker-clearance-2026-06-03.md`, `references/wolfy-kanban-review-batch-clearance-2026-06-04.md`, and `references/wolfy-eod-p4-review-clearance-2026-06-15.md`.
   - When profile cron jobs use relative `script` paths, verify the wrapper exists under that profile's `scripts/` directory or switch the job to a valid absolute script path. Keeping global and profile wrapper scripts synchronized is a safe reversible repair.
   - If a diagnostic references an older script name after an implementation was renamed, prefer a tiny compatibility wrapper that delegates to the live implementation, then teach the script-only autorepair loop to recreate that wrapper. Verify the wrapper and the live implementation both exit 0 before reporting the issue fixed.
   - For cron-facing helper CLIs that agents may probe manually, prefer a safe read-only default for bare invocations when practical. Example: `alpha_search_pipeline.py` now treats no subcommand as a non-mutating JSON status smoke instead of argparse-failing; mutating actions still require explicit subcommands. Verify both bare and explicit `status` invocations plus the regression test. See `references/wolfy-alpha-search-pipeline-no-subcommand-smoke-2026-06-04.md`.
   - For script-only usage-limit/quota watchdogs in profile-scoped operations, pin usage/context probes to the production profile that owns the cron jobs (for Wolfy, usually `hermes --profile default insights --days 1` even when Mike runs from the `mike` profile). Keep enough persisted `seen` digests that a noisy quota day does not roll old events off the state file and re-alert on the same log lines. Scope auth health probes to the provider actually powering the production cron jobs (for example `hermes --profile default auth list openai-codex`), not bare `hermes auth list`, because listing every provider can probe unrelated credentials such as classic `GITHUB_TOKEN` Copilot entries and create noisy false-positive operations warnings. Auth output alone may miss an active Codex `usage_limit_reached`; if same-day Hermes logs contain provider reset metadata, treat `resets_at > now` (or positive `resets_in_seconds` when no reset epoch is present) as active limit evidence, but do not keep jobs paused after that reset timestamp has passed. Include LLM-driven planner/implementer jobs such as Wolfy's daily optimizer in the gated job set, while leaving no-agent/script-only scanners, watchdogs, embedding sync, cleanup, and autorepair active. After changing the watchdog, run it twice: first may emit existing new alerts or a pause/resume transition, second should be silent unless genuinely new quota lines appeared. See `references/profile-scoped-usage-watchdog-dedupe.md`, `references/wolfy-usage-watchdog-auth-scope-2026-06-14.md`, and `references/wolfy-usage-watchdog-active-limit-log-gating-2026-06-24.md`.
   - For profile/default cron script wrappers, check every layer that might be invoked: the live implementation, the global wrapper under `/root/.hermes/scripts/`, and profile wrappers under `~/.hermes/profiles/<name>/scripts/`. A profile wrapper can be healthy while a default-profile cron still calls a stale or missing global wrapper. When repairing, update the autorepair script's canonical source and sync the global/profile copies, then verify all wrapper paths directly. For legacy/prefixed cron wrappers, compile the exact cron wrapper plus its delegated live implementation; do not flag a missing live prefixed filename as broken when the wrapper intentionally delegates to an unprefixed implementation such as `wolfy_sentinel_review_context.py` -> `sentinel_review_context.py` or `wolfy_hourly_knowledge_context.py` -> `hourly_knowledge_context.py`. See `references/wolfy-cron-wrapper-compile-and-silent-reporting-2026-06-14.md`.
   - When a compatibility wrapper is safe and generic (for example a renamed Wolfy helper such as `wolfy_embed_knowledge_chunks.py` delegating to `embed_knowledge_chunks.py`), sync it to every operations profile that may run diagnostics or handoffs, not only the profile that currently owns the cron job. In Wolfy operations this means checking the live implementation, `/root/.hermes/wolfy/<legacy>.py`, `/root/.hermes/scripts/<legacy>.py`, `/root/.hermes/profiles/mike/scripts/<legacy>.py`, and `/root/.hermes/profiles/clerky/scripts/<legacy>.py` when relevant. Teach the deterministic autorepair script to preserve the same set so the fix survives future runs.
   - If an autorepair script itself is part of the operations workflow, include that script in its own global-to-profile sync allowlist too. Otherwise profile-scoped diagnostics can fail with a missing autorepair wrapper even while the global cron job works. For Wolfy/Mike `mike_safe_autorepair.py`, patch the canonical global source under `/root/.hermes/scripts/` first, then run it to sync `/root/.hermes/wolfy/` and profile copies; patching only the Wolfy-local copy can be silently overwritten by the self-sync step. Verify by invoking the global wrapper twice (second run silent), compiling every copy, and grepping each copy for the preserved fix. See `references/profile-autorepair-self-sync.md`.
   - When a renamed context helper is used by cron (for example Alpha Search moving to `alpha_search_context.py` while jobs/diagnostics still reference `wolfy_alpha_search_context.py`), preserve a tiny compatibility wrapper in the live Wolfy directory, global scripts directory, and relevant profile script directories. Teach the no-agent autorepair loop to recreate/sync the wrapper and to keep its own Wolfy-local copy current. Smoke-test every wrapper path, then close any context-created `agent_runs` rows with `records_created=0` so verification does not leave stale `started` noise. See `references/wolfy-alpha-context-wrapper-sync.md`.
   - The same wrapper-preservation pattern applies to post-Wolfy reviewer/analyst contexts whose live files are intentionally unprefixed but cron/profile diagnostics may still probe prefixed names: `wolfy_sentinel_review_context.py` -> `sentinel_review_context.py` and `wolfy_yang_technical_context.py` -> `yang_technical_context.py`. Preserve wrappers in Wolfy-local, global scripts, Mike profile, and Yang profile paths as appropriate; add them to `mike_safe_autorepair.py`, run autorepair twice, compile exact wrappers, and verify with `WOLFY_CONTEXT_SMOKE=1` so no `agent_runs.status='started'` rows are created. See `references/wolfy-sentinel-yang-context-wrapper-sync-2026-06-14.md`.
   - Jonah's hourly/autonomous knowledge context has the same legacy-wrapper risk: diagnostics/cron may call `wolfy_hourly_knowledge_context.py` while the live implementation is `hourly_knowledge_context.py`. Preserve the wrapper in Wolfy-local, global scripts, Mike profile, and Clerky profile paths, teach canonical `/root/.hermes/scripts/mike_safe_autorepair.py` to recreate/sync it, run autorepair twice for silence, compile all exact wrappers, and smoke with `WOLFY_CONTEXT_SMOKE=1` while confirming `agent_runs.status='started'` and `agent_tasks.status='in_progress'` counts do not change. See `references/wolfy-jonah-hourly-context-wrapper-preservation-2026-06-19.md`.
   - When a script-only scanner/helper is introduced under a non-legacy live name (for example `intraday_scanner_snapshot.py`) but cron/profile diagnostics invoke a prefixed wrapper name (`wolfy_intraday_scanner_snapshot.py`), use the same all-layer wrapper sync pattern: Wolfy-local compatibility wrapper, global script wrapper, Mike/Clerky profile wrappers, and `mike_safe_autorepair.py` allowlists. Verify `--help` for every path and a small no-agent smoke with relaxed thresholds such as `--min-ranked 0` when a one-symbol smoke may legitimately rank zero rows. See `references/wolfy-intraday-scanner-wrapper-and-accountability-smoke-2026-06-04.md`.
   - For new Wolfy EOD operations helpers such as monitoring/revalidation loops, use strict TDD, keep behavior conservative, add live/global/profile wrappers, teach `mike_safe_autorepair.py` to preserve them, and verify the second autorepair run is silent. Monitoring may flag/reject/demote existing rows from deterministic facts, but it must never promote strategies or execute trades. See `references/wolfy-eod-monitoring-revalidation-wrapper-sync-2026-06-02.md`.
   - For profile-scoped doctor output, remember that cron/gateway profile wrappers may set `HOME` to the profile home (e.g. `/root/.hermes/profiles/<name>/home`). `hermes doctor --fix` may create `~/.local/bin/hermes` and Playwright browser caches under that profile home, not `/root`; verify with the same `hermes --profile <name> doctor` invocation that reported the issue.
   - If `hermes doctor` reports `Playwright Chromium not installed`, the safe repair is `cd /usr/local/lib/hermes-agent && npx playwright install --with-deps chromium`, then re-run doctor for each relevant profile. Treat the resulting browser availability as install state, not as a durable claim that browser tools were broken.
   - For browser automation, perform a tiny navigation smoke test such as `https://example.com` and confirm a successful title/snapshot.
   - For messaging/file delivery, create a small throwaway file under `/tmp`, send it with `MEDIA:/tmp/...` to the configured platform/channel, and report the returned platform/chat/message IDs. If the first attempt fails with Discord `403 Missing Access`, check whether the gateway/platform has reconnected or the home channel was refreshed, then retry before concluding the channel is inaccessible.
   - For inbound Discord tests, distinguish three layers: message exists in Discord, gateway observed it, and Hermes accepted it into an agent session. Check Discord channel history/API, `~/.hermes/logs/gateway.log`, and `~/.hermes/state.db`. If the log says `Unauthorized user`, report that the message reached the gateway but was rejected before agent processing.
   - Report actual versions and actual remaining warnings from tool output.

## Messaging attachment smoke test

Use this pattern when the user asks whether Hermes can “drop a file” into a gateway channel:

```text
write_file('/tmp/hermes-<platform>-file-test.txt', '...short test payload...')
send_message(target='discord:<channel_id-or-home>', message='Test upload\n\nMEDIA:/tmp/hermes-<platform>-file-test.txt')
```

Verification is the `send_message` result, not just the absence of an exception. Capture and report `platform`, `chat_id`, and `message_id` when present. Treat credential/channel permission errors as setup issues; do not encode them as durable tool failures.

More detail: `references/messaging-attachment-smoke-tests.md`.

## Reporting conventions

- Keep the result concise and grouped as: installed, fixed, verified, remaining.
- Distinguish “remaining because credentials/config are missing” from “remaining because system dependencies are missing.”
- Avoid persistent negative claims like “browser is broken.” Say what setup is missing or what smoke test now confirms.

## References

- `references/optional-dependencies-2026-05.md` — concrete dependency-install and verification transcript distilled from a live Hermes runtime cleanup.
- `references/discord-inbound-gateway-checks.md` — how to verify whether a Discord channel message was merely present, gateway-observed, or actually accepted into a Hermes session.
- `references/profile-cron-ops-triage.md` — compact checklist for profile-scoped cron operations, wrapper sync, usage-ledger sync, and interpreting silent no-agent watchdogs.
- `references/profile-scoped-usage-watchdog-dedupe.md` — Mike/Wolfy pattern for usage watchdog dedupe, production-profile pinning, and quiet repeated runs.
- `references/wolfy-usage-watchdog-auth-scope-2026-06-14.md` — avoid false-positive auth noise by scoping watchdog provider checks to the active production model provider instead of enumerating unrelated credentials.
- `references/wolfy-cron-wrapper-compile-and-silent-reporting-2026-06-14.md` — compile-check the exact cron wrapper plus delegated live implementation, and use exact `[SILENT]` output for healthy scheduled Mike ops runs with nothing new.
- `references/wolfy-runtime-compatibility-aliases.md` — safe Wolfy runtime repairs for schema alias drift, smoke-test `agent_runs` cleanup, and document-extraction package verification.
- `references/wolfy-postgres-alias-drift-agent-alpha-scanner-2026-06-12.md` — Postgres compatibility alias pattern for `agent_runs.completed_at`, `alpha_leads` scanner display aliases, and `scanner_results` probe aliases; includes durable autorepair/schema preservation and verification commands.
- `references/wolfy-alpha-leads-market-context-alias-2026-06-12.md` — follow-up alias drift for `alpha_leads.market_context` mirrored from `raw_payload->'market_context'`, including trigger refresh, pipeline initializer preservation, wrapper sync, and exact-query verification.
- `references/wolfy-agent-artifacts-source-final-url-alias-2026-06-20.md` — non-destructive `agent_artifacts.source_final_url` mirror from canonical `source_url`, plus autorepair/profile-sync verification and scratch-probe placeholder pitfall.
- `references/wolfy-context-smoke-mode-no-started-runs-2026-06-04.md` — add env-gated smoke mode for context helpers so operations probes do not open `agent_runs.status='started'` rows.
- `references/wolfy-eod-monitoring-revalidation-wrapper-sync-2026-06-02.md` — TDD + wrapper/autorepair sync pattern for conservative Wolfy EOD monitoring and monthly strategy-demotion helpers.
- `references/wolfy-knowledge-alias-drift-note-type-path-2026-06-04.md` — follow-up alias drift for `knowledge_notes.note_type` and `knowledge_sources.path`, including trigger refresh and autorepair sync verification.
- `references/wolfy-kanban-review-blocker-clearance-2026-06-03.md` — Mike ops pattern for clearing review-only Wolfy Kanban blockers by rerunning cited non-destructive verification, commenting with real output, and completing the card.
- `references/wolfy-embedding-and-ad-hoc-query-triage-2026-06-19.md` — Mike ops pattern for silent embedding gap closure, stale just-due cron timestamps, and classifying Jonah scratch `tmp_*.py` warnings as ad-hoc triage leads vs durable infra drift.
- `references/wolfy-alpha-search-timeout-stale-run-cleanup-2026-06-25.md` — close stale Alpha Search `agent_runs.status='started'` rows after LLM/provider timeouts as blocked with `records_created=0`, verify no stale ledger noise, and avoid fabricating missed reports.
- `references/wolfy-alpha-search-stale-model-error-smoke-2026-06-25.md` — when Alpha Search cron `last_error` mentions the default model but the job is already configured with a fallback model, verify the raw cron config plus direct profile/model smoke before changing anything or alerting.
